import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFC700", // Change this to your desired primary color (hex, rgb, or CSS color)
    },
  },
  typography: {
    fontFamily: "Inter, sans-serif",
  },
});

export default theme;
